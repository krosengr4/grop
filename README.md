# grop
grep in Go! The basic command line tool that searches files for a string argument and prints out matching lines. However, unlike standard grep which searches files synchronously, grop makes use of Goroutines and channels to search through massive directories in no time!
